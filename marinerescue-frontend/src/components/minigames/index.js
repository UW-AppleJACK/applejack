import DemoMinigame from './DemoMinigame';
import HumphreyNet from './HumphreyNet';
import WaveRacer from './WaveRacer';

// Add a minigame by importing it above. Make sure to use the `toNext` prop!

const minigames = {
    DemoMinigame,
    HumphreyNet,
    WaveRacer,
};

export default minigames;
